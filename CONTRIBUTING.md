# 기여 가이드

## 개발 절차

이 프로젝트는 TDD를 사용합니다.

1. 실패하는 테스트를 먼저 작성합니다.
2. 최소 구현으로 테스트를 통과시킵니다.
3. 리팩터링 후 전체 검증을 실행합니다.

```bash
go test ./...
go vet ./...
gofmt -w .
go build ./...
```

핵심 패키지(`scanner`, `cleaner`)의 커버리지는 70% 이상을 유지해야 합니다.

## 변경 사항

- 삭제 동작은 반드시 임시 디렉터리 기반 테스트를 추가합니다.
- OS별 동작은 해당 OS에서 안전하게 검증할 수 있는 테스트를 작성합니다.
- 파괴적인 명령은 테스트에서 `whoami`, `echo` 등 무해한 명령만 사용합니다.
- 사용자 출력은 주입 가능한 writer를 사용합니다.
